apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: huddle-landing-page-with-single-introductory-section
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /HuddleLandingPageWithSingleIntroductorySection
            pathType: Prefix
            backend:
              service:
                name: huddle-landing-page-with-single-introductory-section
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
